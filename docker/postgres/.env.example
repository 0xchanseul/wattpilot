# Copy this file to .env and adjust the values for your local environment.
# .env is git-ignored and must never contain real production secrets.

POSTGRES_DB=wattpilot
POSTGRES_USER=wattpilot
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5432
